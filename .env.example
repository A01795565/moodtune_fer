# Variables de entorno para MoodTune FER (FastAPI/UVicorn)
# Copia este archivo a .env y ajusta los valores segun tu entorno.

# Host/puerto del servidor FER dentro del contenedor o proceso
FER_HOST=0.0.0.0
FER_PORT=8081

# Puerto publico a exponer en el host (docker-compose mapea FER_PUBLIC_PORT:8081)
FER_PUBLIC_PORT=8081

# URL del modelo ONNX (FER+). Puedes dejar la URL por defecto.
FER_ONNX_URL=https://media.githubusercontent.com/media/onnx/models/main/validated/vision/body_analysis/emotion_ferplus/model/emotion-ferplus-8.onnx

# Ruta local dentro del contenedor donde se guardara el modelo (opcional)
#FER_ONNX_PATH=/app/app/src/emotion-ferplus-8.onnx

# Origenes permitidos para CORS (separados por comas). Usa * en desarrollo.
FER_CORS_ORIGINS=*

